<body id="body" class="bg-white text-gray-900">
{{> header_amp}}

<main class="container px-6 lg:px-20 py-20">
  <section>
    <h1 class="text-4xl lg:text-5xl font-bold mb-8">All pages</h1>
    <ol class="list-decimal space-y-2 pl-6">
      {{#each sitemapData}}
          <li><a href="{{this.url}}">{{this.url}}</a></li>
      {{/each}}
    </ol>
  </section>
</main>

{{> footer_amp_tailwind}}
</body>
